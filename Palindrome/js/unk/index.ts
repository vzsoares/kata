/*
Implement a function `isPalindrome(str)` that checks if a string is a palindrome.
- The function should return true if the string reads the same forwards and backwards.
- Ignore case, spaces, and punctuation when checking.
- Return false for empty strings or non-string inputs.
Example:
  isPalindrome("A man a plan a canal Panama"); // returns true
*/

